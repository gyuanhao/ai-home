# 2026-08-06（周四）

## 每日AI资讯草稿（自动化 automation-1785401958745，09:00）
- 生成 `scripts/_draft_news.md`：2026-08-06 共 10 条，含可直接 prepend 的 JS day 对象 + 人类可读列表 + 已跳过旧闻表。
- 当日主线：Jeff Dean 离职谷歌创办 Discovery Loop（股价跌逾5%）、Meta 首款编程智能体 Muse Code、字节 SeedRealtime 上线豆包、曝 DeepSeek 重启二轮融资估值5000亿、京东开源 JoyAI-Video-Edit、Anthropic 自研芯片团队、Qwen-Image-3.0 上线千问平台、荣耀 MagicOS 10 八月版推送、人民日报评AI治理、Sand.ai 千亿级开源 MoE 视频模型。
- 未改动 `js/news-data.js`，未执行 git/部署。

## 流程改进（可复用）
写完草稿后用 node 做三项自动校验，已验证有效：
1. 提取 ```js 代码块 → `JSON.parse` 验证结构合法；
2. 逐条统计 `[...summary].length` 是否 ≤60 字（本次抓出 1 条 63 字并修正）；
3. 用 `news-data.js` 全文 `includes(title)` 做已入库标题查重。
配合此前已固化的「curl 批量校验候选 URL 状态码」，可同时规避 URL 失效/串味与格式问题。
