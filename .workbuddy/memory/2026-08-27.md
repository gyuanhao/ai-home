# 2026-08-27 工作日志

## AI家AI户·每日AI资讯 8/26+8/27 合并部署上线
- **触发**：用户「上线部署」指令（8/27 自动化草稿产产后）。
- **入库**：8/27（周四，10条）+ 8/26（周三，10条，积压补录）合并 prepend 进 js/news-data.js 数组最前。8/27 置顶、8/26 次之、其后接原 8/25。
- **三项修复**：
  1. 8/26 附录 3 个 `http://view.inews.qq.com` 内链（中消协/OpenAI DevDay/IDC）→ `https`，满足注入严格校验。
  2. 8/26 中消协摘要含直引号 U+0022（"系统自动回复"）破坏 JSON → 改全角「」。
  3. **news-data.js 全量重写**：移除每个 item 末 `"source"` key 后的 trailing comma（JS `vm` 容忍、但 `convert_news.py` 的 `json.loads` 拒绝）。用临时 regen 脚本从现有数据重新输出严格合法 JSON，保留 0/4/6/8 缩进风格。
- **校验**：vm 沙箱 eval + `JSON.parse`（严格）双通过；总天数 40、首项 2026-08-27/次项 2026-08-26/三序 2026-08-25、日期非递增、字段顺序一致、标题全局唯一、FFFD 0、新块 URL 全 https、摘要≤60。
- **仙侠同步**：`python xianxia/scripts/convert_news.py` → xianxia/src/data/news.js 生成 208 条。
- **提交+推送**：commit 8ecf37c（4 files, +310/−39）→ `git push origin main`（a58e88f..8ecf37c）→ Cloudflare Pages 自动构建上线。
- **验证**：cache-bust 确认 myaishome.com/js/news-data.js 首项 2026-08-27、次项 2026-08-26 → 部署成功。
- **临时文件**：scripts/_inject_news_tmp.js、scripts/_regen_news_tmp.js 均已删除。
- **经验沉淀**：
  - 注入脚本 `fmtDay` 对每个 item 的最后一个 key（`"source"`）也加了逗号，JS `vm` 容忍但严格 JSON 解析器（Python `json.loads` / Node `JSON.parse`）拒绝。**今后注入脚本的 `fmtDay` 必须对最后一个 key 不加逗号**，或直接用 `JSON.stringify` 输出再做缩进。
  - Edge 缓存刷新约需 60–90s（本次第二次 cache-bust 才读到新版本），验证勿被首次直连旧结果误导。
