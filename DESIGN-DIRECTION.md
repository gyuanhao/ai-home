# AI家AI户 · 视觉方向文档（DESIGN DIRECTION）

> 用途：本文件是 design-master 定的视觉方向，交给 impeccable 执行时的施工蓝图。
> 当前问题：整体布局/交互/视觉过于单调模板化——根因是配色落在通用「天空蓝 SaaS」套路（design-master 配色库第 5 行），且全站千篇一律的相同卡片网格。
> 目标：保留 AdSense/SEO 所需的「静态可读 + 快加载 + 可信」，同时建立品牌个性、打破模板感。

---

## 一、方向定位

**方向名：「晴山」(Qingshan) — 新中式温暖科技**

- 气质：像邻里便利店一样亲切的 AI 导航，不是冷冰冰的硅谷 SaaS。
- 一句场景：一个中文普通用户（英语不熟）晚上在家用手机挑 AI 工具，页面要让他一眼看懂、放心点。
- 调性关键词：亲切、可信、有东方克制美、不花哨。

**为什么是这套方向（而非其他）：**
- 品牌名「AI家AI户」谐音「家家户户」→ 需要"家/邻里"的暖意，不能是纯科技冷调。
- 中文受众 + 东方美学（水墨/留白/虚实）→ 用「霁蓝(水) + 柿橙(火) + 青瓷绿(木)」三色，既可信又区别于西方蓝 SaaS。
- 必须避开 design-master / impeccable 明令禁止的 AI slop：渐变文字、每节 eyebrow 全大写、01/02/03 编号脚手架、侧边条纹边框、装饰玻璃拟态、千篇一律卡片网格。

---

## 二、色彩系统（commitment 级别：full palette，三色各司其职）

| 角色 | 名称 | Hex | 用途 | 五行意象 |
|------|------|-----|------|----------|
| Primary 主色 | 霁蓝/宝蓝 | `#3B5BDB` | 导航/标题/主按钮/链接 | 水·智慧 |
| Primary-dark | 深霁蓝 | `#2536C9` | hover/active | — |
| Accent CTA | 柿橙 | `#F97316` | 行动按钮/强调/价格 | 火·吉祥热情 |
| Accent-dark | 深柿 | `#EA580C` | 橙底白字按钮(保对比) | — |
| Secondary | 青瓷绿 | `#0D9488` | 分类标签/正向指标/图标点缀 | 木·生机 |
| Secondary-dark | 深青瓷 | `#0F766E` | hover | — |
| Background | 净白冷调 | `#F8FAFC` | 页面底（保持，非米色/cream） | — |
| Surface | 纯白 | `#FFFFFF` | 卡片/侧栏 | — |
| Text | 墨 | `#1E293B` | 正文（≈7.5:1 对比，保 WCAG AA） | — |
| Text-secondary | 灰墨 | `#475569` | 辅助文字 | — |
| Border | 浅边 | `#E2E8F0` | 描边 | — |
| Border-strong | 中边 | `#CBD5E1` | 强分隔 | — |

**暗色模式（未来可选，先留 token 结构）：**
`--bg:#0F172A; --surface:#1E293B; --text:#E2E8F0; --primary:#818CF8; --border:#334155;`

**配色纪律：**
- 主色承载 30–60% 表面（导航条、标题、主按钮），不再是"仅链接蓝"。
- 柿橙只用于行动/强调（稀缺才有效），青瓷绿只用于分类语义，不混用。
- 绝不把对比度压到 4.5:1 以下（正文）；橙/绿底白字按钮用 dark 变体保 ≥3:1（大字）。

---

## 三、字体系统（对比轴搭配，打破全无衬线单调）

| 角色 | 字体 | 说明 |
|------|------|------|
| 标题（中文） | **Noto Serif SC（思源宋体）700/900** | 杂志/编辑感，给博客与文章页"人情味"，区别于工具站冷感 |
| 正文 / UI（中文） | PingFang SC / Microsoft YaHei / system sans | 系统字体，零加载、快、SEO 友好 |
| Latin / 数字 | **Inter**（Google Fonts） | 模型名/参数/数字统一观感 |

- 搭配逻辑：衬线(标题) × 无衬线(正文) = 同一对比轴上的反差（impeccable 排版规则），而非"两个相似无衬线"。
- 性能注意：Noto Serif SC 仅用于 h1–h3 与文章标题，用 `font-display: swap`，可自托管于 Cloudflare 减小请求。
- 中文正文行宽 cap 在 65–75ch；标题 `text-wrap: balance`；大标题 `letter-spacing` 不低于 -0.04em。

---

## 四、布局与节奏原则（治"模板化"的核心）

1. **打破相同卡片网格**：
   - 首页：hero（大标题 + 一句价值主张 + 主 CTA）→ 六类入口（大色块/图标，非小卡）→ 精选文章（编辑式列表，非卡片墙）。
   - 模型库：保留网格，但顶部加分类导语区 + 提供「网格 / 列表 / 表格」三视图切换。
   - 博客：杂志式排版（大标题 + 留白 + 引文块 + 代码块），不用统一卡片。
2. **间距节奏非均匀**：hero 用大留白（如 64–96px），卡片区用 16–24px，制造呼吸。
3. **阴阳虚实**：主内容实，背景大量留白；分类区用浅色块区分而非边框划线。
4. **语义化 z-index 尺度**：dropdown 10 / sticky 20 / 遮罩 198 / 抽屉 199 / toast 300（沿用现有 198/199，勿用 9999）。

---

## 五、组件语言

- 圆角：卡片 12px、按钮/标签 pill 或 8px；**绝不**超过 16px（impeccable codex 禁 24+）。
- 阴影：单一定义阴影，不"1px 边框 + 宽阴影"同用。
- 图标：保留现有 stroke-line SVG（已是品牌资产），统一 20px、idle opacity 0.7 / active 1.0。
- 分类标签：用青瓷绿/柿橙做语义色（如"图像"绿、"代码"橙、"免费"柿橙），模型库一眼可辨。
- 动效：仅 hover/微交互（卡片底色/边框/阴影变化），指数减速曲线；**绝不**把内容门控在滚动揭示动画后（保 SEO 抓取）；支持 `prefers-reduced-motion`。

---

## 六、执行护栏（交给 impeccable 的硬约束）

1. **内容必须静态可读**：所有文字在 HTML 内，JS 动效不隐藏内容（AdSense/SEO 优先）。
2. **保留 i18n 的 `.sb-icon`**：任何重生成导航/带 `data-i18n` 标记的操作，必须保留 SVG 图标（历史坑：innerHTML 覆盖会吞图标）。
3. **保留移动抽屉逻辑**：`mobile-nav.js` 的 `#sidebarDrawer`（z-index 199 > 遮罩 198）不动。
4. **改源不改正文**：只改 `css/style.css` + 共享模板（侧栏、博客模板、模型卡、首页 hero），改完用 `generate_model_pages.py` / `gen_sitemap.js` 重生成全站，**不手改 372 个页面**。
5. **对比度 / 可访问性**：正文 ≥4.5:1，占位符同标准；键盘焦点环保留。

---

## 七、交付验收（impeccable 执行后核对）

- [ ] 主色由天空蓝 `#0EA5E9` 换成霁蓝 `#3B5BDB`，且承载 ≥30% 表面
- [ ] 三色（霁蓝/柿橙/青瓷绿）在导航、标签、CTA 上各司其职
- [ ] 博客/文章标题用思源宋体，正文用系统无衬线
- [ ] 首页/模型库/博客三处布局各有节奏，不再是同一卡片网格
- [ ] 全站对比度达标，移动抽屉可正常点开
- [ ] i18n 图标、AdSense/Analytics 脚本、内容静态可读均未破坏
- [ ] 改完用生成脚本重生成，抽样 3 页验证一致
