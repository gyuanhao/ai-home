/* ============================================================
 * AI之家·仙侠游戏化 — 全局配置 (src/config.js)
 * 作用：放整个游戏都会用到的"固定参数"，集中一处方便统一修改。
 * 比如：画布多大、世界多大、各种颜色、移动速度等。
 * 改这里 = 全站跟着变，不用到处翻代码。
 * ============================================================ */

// 把命名空间挂到全局 AIHome 上，其他文件用 AIHome.CONFIG 就能取到
window.AIHome = window.AIHome || {};

AIHome.CONFIG = {
  // —— 画布（游戏画面在网页里占多大）——
  // 16:9 比例，手机会自动缩放（Phaser 自动处理）
  WIDTH: 960,
  HEIGHT: 540,

  // —— 世界尺寸（与完整地图 world_map.jpg 像素尺寸一致）——
  // 完整地图为 2401×1800（原始素材含标识版），玩家在这个范围内走动
  WORLD_WIDTH: 2401,
  WORLD_HEIGHT: 1800,

  // —— 像素风设置 ——
  // 像素风要"硬边"放大（不要平滑），所以开像素模式
  PIXEL_ART: true,

  // —— 玩家移动 ——
  PLAYER_SPEED: 200, // 每秒像素，点击移动时角色走多快

  // —— 颜色（仙侠像素风调色板，后续换 AI 美术时改这里）——
  COLORS: {
    bg: 0x1a2a1a,            // 地图底色（深绿，仙侠山林）
    grass: 0x2d5a2d,         // 草地瓦片
    path: 0x6b5a3e,          // 小路（土黄）
    player: 0xf0997b,        // 玩家（橙）
    location: 0x378add,      // 地点（蓝，未访）
    locationVisited: 0x5dcaa5, // 已访地点（青绿）
    locationTBD: 0x9aa0a6,    // 待开发地点（灰，规划中）
    footprint: 0xf5c4b3,    // 地面脚印（淡橙发光）
  },

  // —— 触发距离（玩家离地点多近就算"到达"）——
  TRIGGER_RADIUS: 60,

  // —— 寻路网格大小（越小越精细但越慢；16 让窄路/桥梁更顺畅）——
  NAV_CELL: 16,
};
